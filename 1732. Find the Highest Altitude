class Solution {
     public int largestAltitude(int[] gain) {
        int maxi = 0;
        int sum = 0;
        for(int altitude: gain) {
            sum += altitude;
            maxi = Math.max(sum, maxi);
        }
        return maxi;
    }
}
